/**
 * RBAC Configuration
 *
 * Toggle between development and production modes for role-based access control.
 *
 * - In DEVELOPMENT mode: All RBAC checks are bypassed for easier testing
 * - In PRODUCTION mode: Full RBAC enforcement is enabled
 *
 * Change this flag based on your environment:
 * - Set to `true` for local development
 * - Set to `false` for production deployment
 */

export const isDevelopmentMode = true;
