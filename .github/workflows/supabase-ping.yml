name: Keep Supabase Awake

on:
  schedule:
    # Every 3 days
    - cron: "0 0 */3 * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl -X GET \
          "https://muxvkcwwclzmuqwyffkj.supabase.co/functions/v1/ping"
